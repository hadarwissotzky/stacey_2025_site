.gstack/
.DS_Store
.shopify-sync.env
.live-check/

# Dependencies & build output (seo-automation, ai-content-updating-app)
node_modules/
.venv/
dist/
.next/
*.log

# Nested projects with their own git history — version them in their own repos
/seo-automation/
/ai-content-updating-app/

# Google service-account key (GA4 + Search Console reporting). Never commit.
*google-key*.json
*service-account*.json

# Local tool scratch (codex session ids, etc). Not shared.
/.context/
.verify*/
.live-check/
.finalcheck/
